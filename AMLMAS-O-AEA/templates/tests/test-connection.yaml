apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "AMLMAS-O-AEA.fullname" . }}-test-connection"
  labels:
{{ include "AMLMAS-O-AEA.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "AMLMAS-O-AEA.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
