apiVersion: v1
kind: Service
metadata:
  name: {{ include "amlmas-o-rtm.fullname" . }}
  labels:
{{ include "amlmas-o-rtm.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "amlmas-o-rtm.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
